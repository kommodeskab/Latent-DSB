# @package _global_

defaults:
  - test
  - override /callbacks: vinyl_metrics
  - override /model: dsb
  - override /data: chime6_unpaired

project_name: 'chime6'
task_name: 'dsb chime6 test'
compile: False
phase: 'test'

num_steps: 10

data:
  batch_size: 1

model:
  scheduler:
    epsilon: 0.1
    target: drift

trainer:
  limit_test_batches: 1000
