# @package _global_

defaults:
  - override /trainer: gpu
  - override /data: noisy
  - override /callbacks: test_metrics
  - override /model: buddy
  - override /logger: default

project_name: 'baselines'
task_name: 'buddy'
compile: False
phase: 'test'

audio_length: 32768
sample_rate: 16000

model:
  num_steps: 10

data:
  batch_size: 8
  num_workers: 12

trainer:
  log_every_n_steps: 100
  max_epochs: -1
  max_steps: -1
  limit_val_batches: 100
  inference_mode: False
