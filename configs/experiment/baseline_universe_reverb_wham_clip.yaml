# @package _global_

defaults:
  - test
  - override /model: baseline_universe
  - override /data: reverb_wham_clip

project_name: 'baselines'
task_name: 'universe++ reverb_wham_clip'
compile: False
phase: 'test'

num_steps: 10
