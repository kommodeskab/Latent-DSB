# @package _global_

defaults:
  - test
  - override /model: baseline_wpe
  - override /data: reverb_unpaired_libri

project_name: 'baselines (libri)'
task_name: 'wpe'
compile: False
phase: 'test'

num_steps: 0 # not needed for WPE model
