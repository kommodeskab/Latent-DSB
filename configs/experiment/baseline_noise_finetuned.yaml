# @package _global_

defaults:
  - test
  - override /model: dsb
  - override /data: noise_unpaired

project_name: 'baselines'
task_name: 'dsb noise finetuned'
compile: False
phase: 'test'

ckpt_filename: "220426110517/model-220426110517:v0"
num_steps: 10

model:
  scheduler:
    epsilon: 1.0
    target: drift
