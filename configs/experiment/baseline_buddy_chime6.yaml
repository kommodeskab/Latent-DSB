# @package _global_

defaults:
  - test
  - override /callbacks: vinyl_metrics
  - override /model: baseline_buddy
  - override /data: chime6_unpaired

project_name: 'baselines'
task_name: 'buddy chime6'
compile: False
phase: 'test'

num_steps: 100

data:
  batch_size: 1

trainer:
  inference_mode: False
  limit_test_batches: 1000
