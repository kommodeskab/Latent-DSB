# @package _global_

defaults:
  - test
  - override /callbacks: vinyl_metrics
  - override /model: baseline_sgmse
  - override /data: chime6_unpaired

project_name: 'baselines'
task_name: 'sgmse chime6'
compile: False
phase: 'test'

num_steps: 10

data:
  batch_size: 1

model:
  task: noise

trainer:
  limit_test_batches: 1000
