# @package _global_

defaults:
  - test
  - override /model: baseline_sgmse
  - override /data: reverb_unpaired

project_name: 'baselines'
task_name: 'sgmse'
compile: False
phase: 'test'

test_audio_length: 32768
sample_rate: 16000
