# @package _global_

defaults:
  - override /trainer: gpu
  - override /data: noisy
  - override /callbacks: dsb
  - override /model: dsb_waveform
  - override /logger: default

project_name: 'unpaired-audio-diffusion'
task_name: 'test'
compile: False
phase: 'train'

audio_length: 32000

data:
  batch_size: 2
  num_workers: 8

trainer:
  log_every_n_steps: 50
  max_epochs: -1
  max_steps: -1
