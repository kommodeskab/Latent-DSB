# @package _global_

defaults:
  - test
  - override /model: baseline_dsb_unpaired
  - override /data: reverb_unpaired

project_name: 'baselines'
task_name: 'dsb reverb'
compile: False
phase: 'test'

ckpt_filename: "140426074425/model-140426074425:v0"
num_steps: 10

model:
  scheduler:
    epsilon: 0.1
    target: terminal
