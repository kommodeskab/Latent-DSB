# @package _global_

defaults:
  - test
  - override /model: baseline_dsb_unpaired
  - override /data: reverb_unpaired_libri

project_name: 'baselines (libri)'
task_name: 'dsb reverb unpaired (50)'
compile: False
phase: 'test'

ckpt_filename: "300426081933/model-300426081933:v0"
num_steps: 50

model:
  scheduler:
    epsilon: 0.1
    target: drift
