# @package _global_

defaults:
  - test
  - override /model: paired_dsb
  - override /data: reverb_unpaired

project_name: 'baselines'
task_name: 'dsb paired'
compile: False
phase: 'test'

ckpt_filename: "160426232651/model-160426232651:v0"
num_steps: 10

model:
  scheduler:
    epsilon: 0.1
    target: drift
