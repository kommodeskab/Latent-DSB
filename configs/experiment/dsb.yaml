# @package _global_

defaults:
  - override /trainer: gpu
  - override /data: noisy
  - override /callbacks: dsb
  - override /model: dsb
  - override /logger: default

project_name: 'unpaired-audio-diffusion'
task_name: 'test'
compile: False
phase: 'train'

audio_length: 32768

data:
  batch_size: 1

trainer:
  log_every_n_steps: 10
  max_epochs: 3
