# @package _global_

defaults:
  - test
  - override /model: baseline_sgmse
  - override /data: reverb_wham

project_name: 'baselines'
task_name: 'sgmse reverb_wham'
compile: False
phase: 'test'

num_steps: 10

data:
  batch_size: 1

model:
  task: noise

trainer:
  limit_test_batches: 1000
