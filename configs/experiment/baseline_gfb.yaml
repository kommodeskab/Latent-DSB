# @package _global_

defaults:
  - test
  - override /model: baseline_gfb
  - override /data: reverb_unpaired_libri

project_name: 'baselines (libri)'
task_name: 'gfb (10)'
compile: False
phase: 'test'

num_steps: 10
