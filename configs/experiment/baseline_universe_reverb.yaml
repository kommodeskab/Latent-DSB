# @package _global_

defaults:
  - test
  - override /model: baseline_universe
  - override /data: reverb_unpaired

project_name: 'baselines'
task_name: 'universe++ reverb'
compile: False
phase: 'test'

num_steps: 10
