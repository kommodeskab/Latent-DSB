defaults:
  - learningratemonitor
  - logloss
  - modelcheckpoint
  - loggrad
  - batchespersecond
  - params
  - train_metrics
  # - ema

scheduler:
  _target_: src.callbacks.VisualizeSchedulerCallback
